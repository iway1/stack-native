import type { Control } from 'react-hook-form';

/**
 * A really nice form component.
 * @param _
 * @returns
 */
export function AFormComponentOne(_: {
  control: Control<any>;
  something: string;
}) {
  return null;
}
