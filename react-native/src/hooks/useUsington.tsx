/**
 * A hook that returns "usington".
 * @returns "usington"
 * @param usingtonId The id of the usington.
 *
 */
export function useUsington(usingtonId: string) {
  return 'usington' + usingtonId;
}
